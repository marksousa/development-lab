import React from "react";
import { hot } from "react-hot-loader";

const HelloWorld = () => {
  return (
    <div>
      <h1>Wake Up Neo! Follow the white rabbit!</h1>
    </div>
  );
};

export default hot(module)(HelloWorld);
